## Suspending desbelief

Can I keep a barebones blog/site in this place? Perhaps. But how barebones is enough?

Will syntax highlighting work? At least the way I like it? Likely.

```R
some_data |>
  filter(some_condition) |>
  select(some_variables) |>
  mutate(some_variables> |>
  group_by(some_stuff) |>
  summarise(some_calculation) -> new_data
```
Besides that, can I use images hosted in this place? Will have to check.
